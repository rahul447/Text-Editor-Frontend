# Text Editor Frontend

# Details

    1.App by default will run on 127.0.0.1:4002 (configurable)

# Setup

1. Check the npm packages:

    ```
    npm install
    ```
2. Check the npm packages:

    ```
    bower install
    ```

3. Run App:

    ```
    http-server -a localhost -p 4002 -c-1
    ```

Note : Open the firebug console, to check flow as due to shortage of time , i was not able to add notifications.